import React from 'react';
import IconButton from '@material-ui/core/IconButton';
import Icon from '@material-ui/core/Icon';

const EditButton = () => {
    return(
        <IconButton>
            <Icon className="edit_icons">edit_icon</Icon>
        </IconButton>
    );
}

export default EditButton;